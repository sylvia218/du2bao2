# Google and guest login

V7 retains Google login, email authentication and guest browsing. The recommended safe-upload ZIP excludes `config.js`, so your existing working Supabase and Google settings are not overwritten. See `README.md` for the configuration and test checklist.
